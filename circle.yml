machine:
  java:
    version: oraclejdk8

deployment:
  staging:
    branch: master
    heroku: 
      appname: calculadorabeanlab06

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test

